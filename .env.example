# Copy this file to .env before building: cp .env.example .env
# For production, replace with your own 32-byte key and 16-byte IV.
# Generate secure keys with: openssl rand -hex 16 (for IV) and openssl rand -hex 32 (for key)
ENCRYPT_KEY=MasterOfCoinBackupKey32BytesLong!!
ENCRYPT_IV=16ByteIV12345678
